#
# Per-skill master switches (GitHub #10).
#
# Set a skill to Enabled: false and it stops existing for you, in every sense the game can
# express:
#
#   * it earns no XP, from any source, ever;
#   * none of its sub-skills proc -- no bonus drops, no bonus damage, no passives;
#   * its super ability cannot be readied or activated;
#   * it never shows an XP bar;
#   * it is omitted from /mcstats, and /mcstats <skill> says it is disabled;
#   * it grants no milestone advancement plaques.
#
# A half-disabled skill is worse than none, so all six of those move together. There is no
# partial mode and no per-sub-skill switch.
#
# YOUR LEVEL AND XP ARE NOT TOUCHED. Disabling is a pause, not a reset: nothing is deleted from
# your save, and turning the skill back on returns you to exactly the level you left. This is
# also why disabling a skill is not a way to "respec" -- it cannot give you anything.
#
# ---------------------------------------------------------------------------------------------
# AGILITY WAS RETIRED 2026-08-17 -- there is deliberately no `Agility:` row below
# ---------------------------------------------------------------------------------------------
# It was a CHILD skill whose level was the mean of Parkour, Swimming and Flying, and disabling one
# parent froze that parent's third of the average. All of that is gone: Parkour, Swimming and
# Flying are three ordinary skills now, each owning the movement perks specific to it, and each
# switched on or off here independently with no third skill deriving anything from the result.
#
# An `Agility:` block left over in YOUR copy of this file is harmless -- it is simply never read.
# ---------------------------------------------------------------------------------------------

Alchemy:
    Enabled: true
Archery:
    Enabled: true
Axes:
    Enabled: true
Cooking:
    Enabled: true
Crossbows:
    Enabled: true
Excavation:
    Enabled: true
Fishing:
    Enabled: true
Flying:
    Enabled: true
Herbalism:
    Enabled: true
Hunter:
    Enabled: true
Husbandry:
    Enabled: true
Maces:
    Enabled: true
Mining:
    Enabled: true
Parkour:
    Enabled: true
Repair:
    Enabled: true
Salvage:
    Enabled: true
Smelting:
    Enabled: true
Spears:
    Enabled: true
Stealth:
    Enabled: true
Swimming:
    Enabled: true
Swords:
    Enabled: true
Taming:
    Enabled: true
Tridents:
    Enabled: true
Unarmed:
    Enabled: true
Unarmored:
    Enabled: true
Woodcutting:
    Enabled: true
